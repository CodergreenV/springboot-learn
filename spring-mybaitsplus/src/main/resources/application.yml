server:
  port: 9000
# 配置数据库的连接字符串
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/daily?characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
